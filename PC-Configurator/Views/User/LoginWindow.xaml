<Window x:Class="PC_Configurator.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bejelentkezés" Height="400" Width="450"
        Background="#1E1E1E" Foreground="White"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="DarkInputBox" TargetType="TextBox">
                <Setter Property="Background" Value="#1F1F1F"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#555"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="CaretBrush" Value="White"/>
                <Style.Triggers>
                    <Trigger Property="IsFocused" Value="True">
                        <Setter Property="BorderBrush" Value="#4A90E2"/>
                        <Setter Property="Background" Value="#2B2B2B"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="DarkPasswordBox" TargetType="PasswordBox">
                <Setter Property="Background" Value="#1F1F1F"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#555"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="CaretBrush" Value="White"/>
                <Style.Triggers>
                    <Trigger Property="IsFocused" Value="True">
                        <Setter Property="BorderBrush" Value="#4A90E2"/>
                        <Setter Property="Background" Value="#2B2B2B"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="DarkCheckbox" TargetType="CheckBox">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="Foreground" Value="LightGray"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <DockPanel LastChildFill="True">
                                <Border x:Name="CheckBoxBorder"
                                        Width="18" Height="18"
                                        Background="#1F1F1F"
                                        BorderBrush="#777" BorderThickness="1"
                                        CornerRadius="3" Margin="0,0,8,0"
                                        VerticalAlignment="Center">
                                    <Viewbox Stretch="Uniform">
                                        <Path x:Name="CheckMark"
                                              Data="M 0 5 L 4 9 L 10 1"
                                              StrokeThickness="2"
                                              Stroke="#FFFFFF"
                                              StrokeEndLineCap="Round"
                                              StrokeStartLineCap="Round"
                                              Visibility="Collapsed" />
                                    </Viewbox>
                                </Border>
                                <ContentPresenter VerticalAlignment="Center" />
                            </DockPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="CheckBoxBorder" Property="Background" Value="#4A90E2"/>
                                    <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#4A90E2"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#50E3C2"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#2A2A2A" CornerRadius="10" Padding="20" Margin="10">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Bejelentkezés" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Email cím:" FontSize="14" Margin="0,0,0,4"/>
            <TextBox Name="EmailTextBox" Height="35" Style="{StaticResource DarkInputBox}" Margin="0,0,0,10"/>

            <TextBlock Text="Jelszó:" FontSize="14" Margin="0,0,0,4"/>
            <PasswordBox Name="PasswordBox" Height="35" Style="{StaticResource DarkPasswordBox}" Margin="0,0,0,10"/>

            <CheckBox Name="ShowPasswordCheckBox" Content="Jelszó megjelenítése"
                      Style="{StaticResource DarkCheckbox}"
                      Checked="ShowPasswordCheckBox_Checked"
                      Unchecked="ShowPasswordCheckBox_Unchecked"/>

            <Button Content="Bejelentkezés"
                    Width="180" Height="45" Margin="0,10,0,0"
                    Background="#50E3C2" Foreground="White"
                    FontSize="16" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand"
                    HorizontalAlignment="Center"
                    Click="LoginButton_Click"/>
        </StackPanel>
    </Border>
</Window>